require 'vanity_number'

RSpec.describe VanityNumber, "#stringify" do
    context "given a phone number" do
        it  "rejects any invalid phone numbers" do
            test = VanityNumber.new
        end
        it "sources all possible letter combinations"
        it "returns dictionary word matches"
    end
end
